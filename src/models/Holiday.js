class Holiday {
    constructor(id, title, destination, price) {
        this.id = id;
        this.title = title;
        this.destination = destination;
        this.price = price;
    }
}

module.exports = Holiday;
